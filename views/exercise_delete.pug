extends layout

block content
    h1 #{title}: #{exercise.name}
    p Do you really want to delete this Exercise?
    
    form(method='POST' action='')
      div.form-group
        input#exerciseid.form-control(type='hidden',name='exerciseid', required='true', value=exercise._id )

      button.btn.btn-primary(type='submit') Delete