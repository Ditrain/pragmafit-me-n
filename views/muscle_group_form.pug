extends layout

block content
  h1 #{title}

  form(method='POST' action='')
    div.form-group
      label(for='name') Muscle Group:
      input#name.form-control(type='text', placeholder='Muscle group name' name='name' value=(undefined===muscleGroup ? '' : muscleGroup.name))
      label(for="comments") Comments:
      input#name.form-control(type='text', placeholder='Add comments' name='comments' value=(undefined===muscleGroup ? '' : muscleGroup.comments))
    button.btn.btn-primary(type='submit') Submit

  if errors 
    ul
      for error in errors
        li!= error.msg